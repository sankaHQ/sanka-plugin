interface:
  display_name: "Update Property"
  short_description: "Update property in live Sanka."
  default_prompt: "Use $sanka:update-property to update property in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
