interface:
  display_name: "Delete Location"
  short_description: "Delete location from live Sanka."
  default_prompt: "Use $sanka:delete-location to delete location from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
