interface:
  display_name: "List Invoices"
  short_description: "List or search invoices in live Sanka."
  default_prompt: "Use $sanka:list-invoices to list or search invoices in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
