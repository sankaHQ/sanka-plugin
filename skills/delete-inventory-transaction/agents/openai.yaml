interface:
  display_name: "Delete Inventory Transaction"
  short_description: "Delete inventory transaction from live Sanka."
  default_prompt: "Use $sanka:delete-inventory-transaction to delete inventory transaction from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
