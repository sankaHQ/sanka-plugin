interface:
  display_name: "Get Item"
  short_description: "Load one item from live Sanka."
  default_prompt: "Use $sanka:get-item to load one item from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
