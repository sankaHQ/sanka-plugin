interface:
  display_name: "Update Expense"
  short_description: "Update expense in live Sanka."
  default_prompt: "Use $sanka:update-expense to update expense in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
