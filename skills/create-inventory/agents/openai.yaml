interface:
  display_name: "Create Inventory"
  short_description: "Create inventory in live Sanka."
  default_prompt: "Use $sanka:create-inventory to create inventory in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
