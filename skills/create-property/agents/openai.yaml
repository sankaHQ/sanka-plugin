interface:
  display_name: "Create Property"
  short_description: "Create property in live Sanka."
  default_prompt: "Use $sanka:create-property to create property in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
