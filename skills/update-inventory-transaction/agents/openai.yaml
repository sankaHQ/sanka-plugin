interface:
  display_name: "Update Inventory Transaction"
  short_description: "Update inventory transaction in live Sanka."
  default_prompt: "Use $sanka:update-inventory-transaction to update inventory transaction in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
