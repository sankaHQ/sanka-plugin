interface:
  display_name: "Create Inventory Transaction"
  short_description: "Create inventory transaction in live Sanka."
  default_prompt: "Use $sanka:create-inventory-transaction to create inventory transaction in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
