interface:
  display_name: "Create Purchase Order"
  short_description: "Create purchase order in live Sanka."
  default_prompt: "Use $sanka:create-purchase-order to create purchase order in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
