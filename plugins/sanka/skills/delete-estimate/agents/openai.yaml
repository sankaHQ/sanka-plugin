interface:
  display_name: "Delete Estimate"
  short_description: "Delete estimate from live Sanka."
  default_prompt: "Use $sanka:delete-estimate to delete estimate from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
