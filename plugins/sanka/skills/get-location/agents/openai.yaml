interface:
  display_name: "Get Location"
  short_description: "Load one location from live Sanka."
  default_prompt: "Use $sanka:get-location to load one location from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
