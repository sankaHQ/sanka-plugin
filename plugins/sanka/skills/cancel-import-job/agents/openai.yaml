interface:
  display_name: "Cancel Import Job"
  short_description: "Cancel import job in live Sanka."
  default_prompt: "Use $sanka:cancel-import-job to cancel import job in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
