interface:
  display_name: "Get Calendar Bootstrap"
  short_description: "Load one calendar bootstrap from live Sanka."
  default_prompt: "Use $sanka:get-calendar-bootstrap to load one calendar bootstrap from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
