interface:
  display_name: "Update Purchase Order"
  short_description: "Update purchase order in live Sanka."
  default_prompt: "Use $sanka:update-purchase-order to update purchase order in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
