interface:
  display_name: "Download Order PDF"
  short_description: "Download order PDF from live Sanka."
  default_prompt: "Use $sanka:download-order-pdf to download order PDF from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sanka_plugin"
      description: "Hosted Sanka MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
