interface:
  display_name: "Delete Bill"
  short_description: "Delete bill from live Sanka."
  default_prompt: "Use $sanka:delete-bill to delete bill from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sakura"
      description: "Hosted Sakura MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
