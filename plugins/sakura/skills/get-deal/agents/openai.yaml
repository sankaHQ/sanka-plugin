interface:
  display_name: "Get Deal"
  short_description: "Load one deal from live Sanka."
  default_prompt: "Use $sanka:get-deal to load one deal from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sakura"
      description: "Hosted Sakura MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
