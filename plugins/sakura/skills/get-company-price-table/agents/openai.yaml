interface:
  display_name: "Get Company Price Table"
  short_description: "Load one company price table from live Sanka."
  default_prompt: "Use $sanka:get-company-price-table to load one company price table from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sakura"
      description: "Hosted Sakura MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
