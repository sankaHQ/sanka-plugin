interface:
  display_name: "Create Estimate"
  short_description: "Create estimate in live Sanka."
  default_prompt: "Use $sanka:create-estimate to create estimate in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sakura"
      description: "Hosted Sakura MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
