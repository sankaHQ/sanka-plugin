interface:
  display_name: "Get Import Job"
  short_description: "Load one import job from live Sanka."
  default_prompt: "Use $sanka:get-import-job to load one import job from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sakura"
      description: "Hosted Sakura MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
