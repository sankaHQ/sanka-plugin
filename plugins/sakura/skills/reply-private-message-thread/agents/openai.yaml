interface:
  display_name: "Reply Private Message Thread"
  short_description: "Reply to a private inbox thread in live Sanka."
  default_prompt: "Use $sanka:reply-private-message-thread to reply to a private inbox thread in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sakura"
      description: "Hosted Sakura MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
