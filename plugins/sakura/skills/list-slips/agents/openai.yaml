interface:
  display_name: "List Slips"
  short_description: "List or search slips in live Sanka."
  default_prompt: "Use $sanka:list-slips to list or search slips in live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sakura"
      description: "Hosted Sakura MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
