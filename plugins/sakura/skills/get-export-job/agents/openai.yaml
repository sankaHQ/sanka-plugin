interface:
  display_name: "Get Export Job"
  short_description: "Load one export job from live Sanka."
  default_prompt: "Use $sanka:get-export-job to load one export job from live Sanka."

dependencies:
  tools:
    - type: "mcp"
      value: "sakura"
      description: "Hosted Sakura MCP server for live Sanka workflows"
      transport: "streamable_http"
      url: "https://mcp.sanka.com/mcp"

policy:
  allow_implicit_invocation: false
